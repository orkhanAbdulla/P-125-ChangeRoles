@model Category
@{
    ViewData["Title"] = "Detail";
}

<div class="container">
    <div class="row">
        <div class="col-6">
            <h5>@Model.Name</h5>
            <p>@Model.Description</p>
        </div>
        <div class="col-6">
           <a asp-controller="Category" asp-action="Index" class="btn btn-warning">Go Back</a>
        </div>
    </div>
</div>

